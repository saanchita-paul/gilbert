export default class OfficeMetrics{

    constructor({application_metrics, utility_metrics}) {
        this.application_metrics = application_metrics;
        this.utility_metrics = utility_metrics;
    }
}
